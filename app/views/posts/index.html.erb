<section class="section">
  <div class="container">
    <div class="columns">
    	<% if user_signed_in? %>
    		<%= render 'profile' %>
        <%= render 'feed' %>
      <% else %>
    		<%= render 'without' %>
      <% end %>
       
    </div>
  </div>
</section>